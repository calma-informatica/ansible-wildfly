### Pré requisitos para instalação {docsify-ignore}

As seguintes ferramentas devem estar instaladas na máquina *bastion*
- git
- wget
- ansible

Para instalar basta executar o comando abaixo:
```
sudo dnf install git wget ansible
```
